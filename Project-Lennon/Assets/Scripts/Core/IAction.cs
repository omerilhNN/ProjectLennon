namespace RPG.Core
{
    public interface IAction
    {
     //You could only use methods that have have already implemented in the 
     //other classes. Can't implement something new in here.
     //ALL THESE REFERENCES MUST HAVE SAME NAMES.

        void Cancel();

    }
}